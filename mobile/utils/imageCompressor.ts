/**
 * Placeholder for image compression (e.g. using expo-image-manipulator or similar).
 * Returns the URI as-is for now.
 */
export async function compressImage(uri: string): Promise<string> {
  return uri;
}
